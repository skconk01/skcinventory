
This directory should be used to place downloaded and custom modules
which are common to all sites. This will allow you to more easily
update Drupal core files.


added the following directories



Now the logic behind this is that any modules that you download off of Drupal. org and use directly without modifying anything goes into contrib.

-->> contrib

Any modules that are built specifically for this site that you are working on, or that you use internally, those modules will go into the custom folder.

-->> custom


And then any feature modules, or modules created with the feature module, go into the features folder. So lets go ahead and start with some example files. What we are going to do is go through a basic file structure for a module.  http://drupal.org/project/features

-->>features

